# ePixC ESP32-S3, 8 MB flash. Two 3 MB OTA slots, 1.875 MB filesystem.
#
# WLED's shared WLED_ESP32_8MB.csv gives 2 MB app slots and a 3.875 MB filesystem.
#
# To be accurate about why this changed: the current ePixC image is 983 KB, so it fits a 2 MB slot
# with room to spare. Tickets 16 and 29 both assumed ~1.8 MB; that was wrong, and measured here at
# 31.3% of 3 MB. The reason to widen the slots anyway is that a partition table cannot be changed
# over OTA, so the choice is permanent per unit, and 3 MB keeps options open (Matter needs roughly
# 1.5 MB on top of WLED) without physically reflashing boards later. The filesystem gives up
# 2 MB it was not using.
#
# THIS CANNOT BE CHANGED OVER OTA. A partition table is written by the flasher, not by an update,
# so once a unit ships its layout is fixed for life. No unit has shipped, which makes this the last
# free moment to widen the slots. Layout follows the proven ESP32-S3 arrangement from WLED
# PR #5456.
#
# Reflashing this table ERASES the filesystem: cfg.json, wsec.json and presets go with it, so a
# bench unit comes back needing provisioning and pairing again. That is the same end state as a
# factory reset, and correct.
#
# Arithmetic, so the next person does not have to redo it:
#   app0     0x010000 + 0x300000 = 0x310000   3.000 MB
#   app1     0x310000 + 0x300000 = 0x610000   3.000 MB
#   spiffs   0x610000 + 0x1E0000 = 0x7F0000   1.875 MB
#   coredump 0x7F0000 .. 0x800000               64 KB   (8 MB total)
# Both app offsets are 64 KB aligned, which the ESP32 bootloader requires.
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x300000,
app1,     app,  ota_1,   0x310000, 0x300000,
spiffs,   data, spiffs,  0x610000, 0x1E0000,
coredump, data, coredump,,         64K
