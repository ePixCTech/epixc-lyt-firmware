.cache
.clang-format
.direnv
.DS_Store
.idea
.pio
.pioenvs
.piolibdeps
.vscode
compile_commands.json
__pycache__/

/.dummy
/dependencies.lock
/managed_components

esp01-update.sh
# Upstream ignores this as a per-developer override. For ePixC it IS the product
# definition — partition table, GPIO map, LED type, every WLED_DISABLE_* flag — so
# losing it means losing the ability to build the firmware that ships. Tracked.
!platformio_override.ini
platformio_release.ini
replace_fs.py
wled-update.sh

/build_output/
/node_modules/
/logs/

/wled00/extLibs
/wled00/LittleFS
/wled00/my_config.h
/wled00/Release
/wled00/wled00.ino.cpp
/wled00/html_*.h
/wled00/js_*.h
